#include "iostream"
#include "cstdlib"
#include "math.h"

using namespace std;

 int num =6, val=0; 


int main()
{/*
        int seq[] ={1,2,3,4,5,6}; 
        while(num > 0){ 
            val += seq[num-1];
            num--; 
    }
        cout<<"Valor calculado para Val: "<<val;
    return 0;
*/

/*
int num, val, aux; 
        num = 5; 
        aux = num; 
        val = 1; 
        while(aux > 1){ 
            val = val * (aux);
            aux--; 
        } 
cout<<"Valor calculado para "<<num<<" é: "<<val;
*/


int a,b,c;
a=3;
b=4;
c=a+++b--;

cout<<"Valor de C: "<<c;
}
